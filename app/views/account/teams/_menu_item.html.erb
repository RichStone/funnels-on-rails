<%= render 'account/shared/menu/item', {
  url: main_app.polymorphic_path([:account, current_team]),
  label: t('menus.main.labels.dashboard'),
} do |item| %>
  <% item.icon do %>
    <i class="fal fa-star ti ti-star"></i>
  <% end %>
<% end %>

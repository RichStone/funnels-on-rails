<div class="mx-auto max-w-[600px]">
  <% if current_user.subscription_status == User::SUBSCRIPTION_STATUSES[:premium] %>
    <div class="mb-4 p-4 bg-yellow-50 rounded-lg border border-yellow-200">
      <h3 class="font-medium text-yellow-800">This is your Premium badge ⭐️</h3>
      <p class="mt-2 text-black">
        Shoot Rich an email to chat about your most important question in the 1-on-1 consultation if you haven't done it yet:
        <a href="mailto:hey@richsteinmetz.com" class="text-blue-600 hover:text-blue-800">hey@richsteinmetz.com</a> ✉️
        Thanks for your trust! And please cancel your subscription! 😃 You can do so in
        <a href="https://www.funnelsonrails.com/contacts/sign_in" class="text-blue-600 hover:text-blue-800" target="_blank" rel="noopener">the Funnels on Rails Customer Center</a>.
      </p>
    </div>
  <% end %>

  <div class="mb-6 p-4 bg-white dark:bg-slate-700 rounded-lg border border-gray-200 dark:border-slate-600">
    <p class="text-xs font-medium text-gray-400 dark:text-gray-500 uppercase tracking-wide mb-2">Your ONE Business</p>
    <% if @business.persisted? %>
      <div class="space-y-3">
        <div class="flex items-center justify-between">
          <h2 class="text-2xl font-bold text-gray-800 dark:text-gray-100"><%= @business.name %></h2>
          <%= link_to edit_account_business_path(@business), class: "text-gray-400 hover:text-primary-600 dark:text-gray-500 dark:hover:text-primary-400 ml-2", title: "Edit" do %>
            <i class="fal fa-pencil ti ti-pencil"></i>
          <% end %>
        </div>
        <% if @business.description.present? %>
          <div class="text-gray-600 dark:text-gray-300 prose dark:prose-invert max-w-none">
            <%= @business.description %>
          </div>
        <% end %>
        <% if @business.funnel_url.present? || @business.app_url.present? %>
          <div class="flex flex-wrap gap-3 pt-2">
            <% if @business.funnel_url.present? %>
              <%= link_to @business.funnel_url, class: "inline-flex items-center text-sm text-primary-600 hover:text-primary-800 dark:text-primary-400", target: "_blank", rel: "noopener" do %>
                <i class="fal fa-funnel-dollar mr-1"></i> Funnel
              <% end %>
            <% end %>
            <% if @business.app_url.present? %>
              <%= link_to @business.app_url, class: "inline-flex items-center text-sm text-primary-600 hover:text-primary-800 dark:text-primary-400", target: "_blank", rel: "noopener" do %>
                <i class="fal fa-browser mr-1"></i> App
              <% end %>
            <% end %>
          </div>
        <% end %>
      </div>
    <% else %>
      <div class="flex items-center justify-between">
        <p class="text-gray-500 dark:text-gray-400">Add your business to get started!</p>
        <%= link_to new_account_team_business_path(@team), class: "inline-flex items-center px-3 py-2 border border-transparent text-sm leading-4 font-medium rounded-md text-white bg-primary-600 hover:bg-primary-700" do %>
          <i class="fal fa-plus mr-2"></i> Add
        <% end %>
      </div>
    <% end %>
  </div>

  <% if @random_tip.present? %>
    <div class="mb-4 p-4 dark:bg-blue-50 bg-gray-50 rounded-lg">
      <h3 class="font-medium text-blue-800">Today's Developer Marketing Tip:</h3>
      <p class="mt-2 text-black"><%= @random_tip %></p>
    </div>
  <% end %>

  <% if @random_reason.present? %>
    <div class="mb-4 p-4 dark:bg-amber-50 bg-gray-50 rounded-lg">
      <h3 class="font-medium text-amber-800">
        Today's reason to stop using Rails before it's too late:
      </h3>
      <p class="mt-2 text-black"><%= @random_reason %></p>
      <br>
      <%= image_tag("stop-using-rails-reasons/funnelsonrails-wide.png", class: "w-full") %>
    </div>
  <% end %>

  <%# 🚅 super scaffolding will insert new children above this line. %>
</div>
